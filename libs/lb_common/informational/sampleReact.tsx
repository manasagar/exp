import React from 'react';
import { ScrollView } from 'react-native';
import tw from 'twrnc'; // Using the project's styling framework

export const AboutUs = () => {
  return <ScrollView style={tw`bg-white dark:bg-[#151718]`}></ScrollView>;
};

export default AboutUs;
