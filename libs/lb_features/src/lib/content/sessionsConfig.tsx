import { instructionConfig } from './data/nudgeCards';

export const nonSessionsConfig = {
  0: [{ items: instructionConfig.items.mvp, position: 2 }],
};

export const sessionsConfig = {
  0: 'ergonomicsData',
  1: 'focusData',
  2: 'planningData',
  3: 'productivityData',
};
