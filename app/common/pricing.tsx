import PricingContainer from '@/libs/lb_common/pricing/pricingContainer';
import * as React from 'react';
import { ScrollView } from 'react-native';

export default function Pricing() {
  return (
    <ScrollView>
      <PricingContainer />
    </ScrollView>
  );
}
