import React from 'react';
import { View } from 'react-native';
import RefundPolicyContainer from '@/libs/lb_common/informational/refundPolicyContainer';

export default function RefundPolicyScreen() {
  return (
    <View style={{ flex: 1 }}>
      <RefundPolicyContainer />
    </View>
  );
}
