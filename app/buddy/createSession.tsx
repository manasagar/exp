import CreateSessionContainer from '@/libs/lb_buddy/src/lib/createSession/createSessionContainer';
import * as React from 'react';

export default function createSession() {
  return <CreateSessionContainer />;
}
